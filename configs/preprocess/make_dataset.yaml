preprocess:
  dataset_name: lakh_matched
  midi_root: "/home/ec2-user/data/lmd_matched"
  # Output location (prefix for shard filenames)
  dump_path: "/home/ec2-user/data/parquet"
  only_drum: true

  partitions: null